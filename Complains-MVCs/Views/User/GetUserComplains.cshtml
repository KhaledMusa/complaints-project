@model IEnumerable<Complains_MVCs.Models.FileComp>


@{
    ViewData["Title"] = "GetUserComplains";
    Layout = "~/Views/Shared/_Layout.cshtml";
}

<h1>Complains</h1>

@* @if (!string.IsNullOrEmpty(Model))
{
    <script>
        // Parse the JSON data received from the controller action
        var userData = @Html.Raw(Model);



        // Check if userData is not empty (user is logged in)
        if (userData) {
            // Save user data in session storage
            localstorage.setItem('userData', JSON.stringify(userData));
            console.log('User data saved in session storage:', userData);
        }
    </script>
} *@
<div >
    <a asp-action="Create" asp-controller="User" class="btn btn-primary">Create</a>
</div>
<div class="text-center">
    <a asp-action="MyProfile" asp-controller="User" class="btn btn-primary">My Profile</a>
</div>
<hr />
<table class="table table-striped table-dark table-bordered">

    <thead>
        <tr>
           
            <th>
                @Html.DisplayNameFor(model => model.Text)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.ContentType)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Status)
            </th>
            
            <th></th>
        </tr>
    </thead>
    <tbody>
        @foreach (var item in Model)
        {
            <tr>
                
                <td>
                    @Html.DisplayFor(modelItem => item.Text)
                </td>
                <td>
                    @Html.DisplayFor(modelItem => item.ContentType)
                </td>
                <td>
                    @Html.DisplayFor(modelItem => item.Status)
                </td>
               
                <td class="text-center">
                    <a asp-action="Details" asp-controller="User" asp-route-id="@item.Id" class="btn btn-outline-info">Details</a>


                    <a asp-action="Edit" asp-controller="User" asp-route-id="@item.Id" class="btn btn-outline-success">Accepted</a>


                   
                </td>
            </tr>
        }
    </tbody>
</table>
