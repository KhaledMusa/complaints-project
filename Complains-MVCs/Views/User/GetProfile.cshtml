@model Complains_MVCs.Models.User


@{
    ViewData["Title"] = "Profile";
    Layout = "~/Views/Shared/_Layout.cshtml";
}

<h1>Complains</h1>

@* @if (!string.IsNullOrEmpty(Model))
{
    <script>
        // Parse the JSON data received from the controller action
        var userData = @Html.Raw(Model);



        // Check if userData is not empty (user is logged in)
        if (userData) {
            // Save user data in session storage
            localstorage.setItem('userData', JSON.stringify(userData));
            console.log('User data saved in session storage:', userData);
        }
    </script>
} *@

<hr />
<table class="table table-striped table-dark table-bordered">

    <thead>
        <tr>
            
            <th>
                @Html.DisplayNameFor(model => model.UserName)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Email)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Phone)
            </th>
            
            <th></th>
        </tr>
    </thead>
    <tbody>
        
        
            <tr>
                
                <td>
                    @Html.DisplayFor(modelItem => modelItem.UserName)
                </td>
                <td>
                    @Html.DisplayFor(modelItem => modelItem.Email)
                </td>
                <td>
                    @Html.DisplayFor(modelItem => modelItem.Phone)
                </td>
                <td class="text-center">
                <a asp-action="UpdateComp" asp-controller="User" asp-route-id="@Model.Id" class="btn btn-outline-warning">Edit</a>
                </td>
              
            </tr>
        
    </tbody>
</table>
<div>
    <a asp-action="GetUserComplains">Back</a>
</div>