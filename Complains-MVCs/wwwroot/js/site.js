// Please see documentation at https://docs.microsoft.com/aspnet/core/client-side/bundling-and-minification
// for details on configuring this project to bundle and minify static web assets.

// Write your JavaScript code.
//const user = window.location.hash.substr(1); // Get the fragment from the URL

//if (user.startsWith("complaint-")) {
//    const complaintId = user.replace("complaint-", "");
//    const complaintSection = document.getElementById(`complaint-${complaintId}`);
//    if (complaintSection) {
//        complaintSection.scrollIntoView({ behavior: "smooth" });
//    }
//}
